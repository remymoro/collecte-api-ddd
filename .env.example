# Exemple DEV (PostgreSQL local ou Docker)
#
# Si tu utilises `docker/dev/docker-compose.yml` (db exposée sur localhost:5432):
#
#   docker compose -f docker/dev/docker-compose.yml up -d
#
# Alors tu peux utiliser une URL PostgreSQL classique :
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/collecte?schema=public"

# Optionnel (utile si tu veux aussi réutiliser ces valeurs dans Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=collecte
